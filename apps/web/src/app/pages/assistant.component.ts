import { CommonModule } from '@angular/common';
import { Component, effect, inject, signal } from '@angular/core';
import { FormsModule } from '@angular/forms';
import { ApiService } from '../core/api.service';import { WorkspaceStore } from '../core/workspace.store';import type { AssistantResponse } from '../models';
@Component({standalone:true,imports:[CommonModule,FormsModule],template:`
<div class="page-head"><div><p class="eyebrow">PHASE 10 · DELIVERY ASSISTANT</p><h1>Delivery assistant</h1><p class="muted">A deterministic, evidence-based delivery copilot built from live workspace data.</p></div></div>
<section class="assistant-layout"><article class="card settings-card"><label>Focus<select [(ngModel)]="focus"><option value="delivery">Delivery</option><option value="risks">Risks</option><option value="team">Team</option><option value="sprint">Sprint</option><option value="executive">Executive</option></select></label><label>Ask about the workspace<textarea [(ngModel)]="question" placeholder="What should the team focus on next?"></textarea></label><button class="btn primary" (click)="ask()">Analyse workspace</button></article>
@if(result();as r){<article class="card settings-card assistant-result"><span class="pill">{{r.mode}}</span><h2>{{r.summary}}</h2><h3>Insights</h3>@for(x of r.insights;track x){<p>• {{x}}</p>}<h3>Recommended actions</h3>@for(x of r.recommendations;track x){<p>• {{x}}</p>}<div class="chip-list"><span class="pill">{{r.metrics.blocked}} blocked</span><span class="pill">{{r.metrics.overdue}} overdue</span><span class="pill">{{r.metrics.in_review}} in review</span></div></article>}</section>`})
export class AssistantComponent{private readonly api=inject(ApiService);readonly store=inject(WorkspaceStore);readonly result=signal<AssistantResponse|null>(null);focus='delivery';question='What should the team focus on next?';constructor(){effect(()=>{if(this.store.current())this.ask();});}ask(){const w=this.store.current();if(!w)return;this.api.assistant(w.id,{focus:this.focus,question:this.question}).subscribe(v=>this.result.set(v));}}